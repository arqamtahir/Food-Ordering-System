<section class="page-content container-fluid">
  <div class= "row float-right ml-auto mb-2 mr-3">          
    <%= link_to new_food_item_path,class:"btn btn-info btn-outline btn-floating btn-rounded mt-2" do %>
      <span>+ Add Food Item</span>
    <% end %>

    <%= link_to discarded_food_items_path	,class:"btn btn-info btn-outline btn-floating btn-rounded mt-2 ml-2" do %>
      <span> Recycle Bin</span>
    <% end %>
  </div>
  
  <div class="row">
    <div class="col-12">
      <div class="card shadow p-3 mb-3 rounded form">        
        <div class= "row">
          <div class="col-6">
            <h1 class="card-header ml-2">Food Items</h1>          
          </div>
          
          <%# render partial for filteration %>
          <%= render "shared/filteration"%>
        </div>
        
        <div class="card-body ">
          <table id="bs4-table" class="table table-striped table-bordered" style="width:100%">
            <thead>
              <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Status</th>
                <th scope="col">Actions</th>
              </tr>
            </thead>

            <tbody>
              <% @food_items.each do |food_item| %>
                <tr>
                  <td><%=food_item.id%></td>
                  <td><%=food_item.name%></td>
                  <td><%=food_item.post_status%></td>

                  <td>
                    <%= link_to edit_food_item_path(food_item) do%>
                      <i class="la la-pencil-square btn btn-info btn-outline btn-sm btn-floating btn-rounded"></i>
                    <% end %>
                    
                    <%= link_to food_item_path(food_item) do%>
                      <i class="la la-eye btn btn-success btn-outline btn-sm btn-floating btn-rounded"></i>
                    <% end %>
                    
                    <%= link_to food_item_path(food_item), method: :delete, data: {confirm: 'Are you sure?'} do%>
                      <i class="la la-close btn btn-danger btn-outline btn-sm btn-floating btn-rounded"></i>
                    <% end %>
                  </td>
                </tr>
							<% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</section>
